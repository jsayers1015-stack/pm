services:
  app:
    build:
      context: .
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - .env
    environment:
      DB_PATH: /data/pm.db
      STATIC_DIR: /app/static
    volumes:
      - pm-data:/data

volumes:
  pm-data:
