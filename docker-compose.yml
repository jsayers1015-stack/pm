services:
  app:
    build:
      context: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      DB_PATH: /data/pm.db
      STATIC_DIR: /app/static
    volumes:
      - pm-data:/data

volumes:
  pm-data:
